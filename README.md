
  # Chord Selector Application

  This is a code bundle for Chord Selector Application. The original project is available at https://www.figma.com/design/NOoxImScZ7zaWpPthcEAcB/Chord-Selector-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
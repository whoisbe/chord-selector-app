// Loop 006 replaces the Loop 004 smoke surface with the real two-row keyboard
// input. The tab itself is now just the mount point.

import { PhraseLookupSurface } from './phrase-lookup/PhraseLookupSurface';

export function PhraseLookupTab() {
  return <PhraseLookupSurface />;
}
